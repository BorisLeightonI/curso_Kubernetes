apiVersion: v1
kind: Pod
metadata:
  name: "nginx-always"
  namespace: default
  labels:
    app: "nginx-always"
spec:
  containers:
    - name: nginx-always
      image: "nginx:latest"
      ports:
        - containerPort: 80
          name: http
  restartPolicy: Always
