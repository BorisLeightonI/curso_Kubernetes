apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: ingress-for-nginx
spec:
  rules:
    - http:
        paths:
          - path: /v1
            pathType: Prefix
            backend:
              service:
                name: nginx-service-for-ingress
                port:
                  number: 80
